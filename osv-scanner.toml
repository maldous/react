# OSV-Scanner accepted-risk register (audit:osv gate, make/quality.mk `audit`).
#
# Every entry below is a DEV-ONLY dependency advisory: the package is a build/test/
# lint tool, NOT part of any shipped artifact (the platform-api Docker image and the
# SPA production bundle ship PRODUCTION deps only — `npm audit --omit=dev` reports 0
# vulnerabilities). These are recorded as accepted risk so the `make all` audit gate
# is deterministic (it otherwise fails network-dependently on transient dev-tool CVEs)
# while still failing hard on any PRODUCTION vulnerability or a NEW advisory not listed
# here. Re-review when bumping the owning dev tool; drop the entry once patched.
# Reviewed: 2026-06-16. Amended 2026-06-19 (3 dev-only advisories added below, time-bounded).
# Amended 2026-06-20 (6 further dev-only advisories added at the foot, time-bounded).
# Owner: Architecture owner / platform. Time-bounded entries carry `ignoreUntil`; the gate
# re-fails automatically once the expiry passes, forcing re-review (upgrade or re-justify).

[[IgnoredVulns]]
id = "GHSA-fx2h-pf6j-xcff"
reason = "vite (dev) — dev-server server.fs.deny bypass on Windows alternate paths. Build/dev tool only; never serves production traffic."

[[IgnoredVulns]]
id = "GHSA-v6wh-96g9-6wx3"
reason = "vite/launch-editor (dev) — NTLMv2 disclosure via UNC paths on Windows in the dev server. Not in the shipped bundle."

[[IgnoredVulns]]
id = "GHSA-vmf3-w455-68vh"
reason = "tar (dev) — PAX header parser differential in build tooling. Not a runtime dependency of the deployed services."

[[IgnoredVulns]]
id = "GHSA-6v5v-wf23-fmfq"
reason = "markdown-it (dev, via markdownlint-cli2) — quadratic-complexity DoS in the docs linter. Dev-time only."

[[IgnoredVulns]]
id = "GHSA-h67p-54hq-rp68"
reason = "js-yaml (dev) — used by dev/build tooling only; not loaded by the production services."

[[IgnoredVulns]]
id = "GHSA-8988-4f7v-96qf"
reason = "@opentelemetry/core (dev-resolved at repo root) — the production OTEL stack lives in packages/adapters-opentelemetry with its own pinned deps; the root dev copy is not shipped."

# --- Time-bounded governed exceptions (added 2026-06-19; re-fail after ignoreUntil) ---
# Exposure: `npm audit --omit=dev` reports 0 PRODUCTION vulnerabilities; all three below are
# dev-only and absent from every shipped artifact. Owner: Architecture owner / platform.
# Remediation path tracked under ADR-ACT-0292 / V1C-18 (authoritative dependency-scan gate).

[[IgnoredVulns]]
id = "GHSA-p6gq-j5cr-w38f"
ignoreUntil = 2026-09-19T00:00:00Z
reason = "nodemailer@8.0.9 (dev, direct devDependency) — used only by dev/test email tooling; production email is sent through @platform/adapters-brevo (EmailTransportAdapter), never this root dev copy. Not in the platform-api image or the SPA bundle. Time-bounded to 2026-09-19; remediate by bumping nodemailer when a fixed release is available (V1C-18)."

[[IgnoredVulns]]
id = "GHSA-pr7r-676h-xcf6"
ignoreUntil = 2026-09-19T00:00:00Z
reason = "undici@7.26.0 (dev, transitive via jsdom@29.1.1) — loaded only inside the jsdom/Vitest test DOM environment; production HTTP uses the Node runtime's own pinned undici, not this dev copy. Not shipped. Time-bounded to 2026-09-19; remediate via jsdom bump (V1C-18)."

[[IgnoredVulns]]
id = "GHSA-vmh5-mc38-953g"
ignoreUntil = 2026-09-19T00:00:00Z
reason = "undici@7.26.0 (dev, transitive via jsdom@29.1.1) — same dev-only test-environment exposure as GHSA-pr7r-676h-xcf6; not in any shipped artifact. Time-bounded to 2026-09-19; remediate via jsdom bump (V1C-18)."

# --- Time-bounded governed exceptions (added 2026-06-20; re-fail after ignoreUntil) ---
# Newly-published advisories surfaced by the OSV feed since the 2026-06-16 review. All are
# DEV-ONLY: `npm audit --omit=dev` reports 0 PRODUCTION vulnerabilities; none reach a shipped
# artifact. Owner: Architecture owner / platform. Remediation tracked under ADR-ACT-0292 / V1C-18.

[[IgnoredVulns]]
id = "GHSA-v75r-vx73-82pj"
ignoreUntil = 2026-09-19T00:00:00Z
reason = "@cyclonedx/cyclonedx-npm@4.2.1 (dev, direct devDependency) — the SBOM generator run by `make sbom`/CI tooling only; never imported by the platform-api image or the SPA bundle. Dev-only. Time-bounded to 2026-09-19; remediate by bumping @cyclonedx/cyclonedx-npm when a fixed release is available (V1C-18)."

[[IgnoredVulns]]
id = "GHSA-35p6-xmwp-9g52"
ignoreUntil = 2026-09-19T00:00:00Z
reason = "undici (dev, transitive via jsdom/test tooling at 6.26.0 & 7.26.0) — loaded only inside the jsdom/Vitest test DOM environment; production HTTP uses the Node runtime's own pinned undici, not this dev copy. Not shipped. Time-bounded to 2026-09-19; remediate via jsdom bump (V1C-18)."

[[IgnoredVulns]]
id = "GHSA-g8m3-5g58-fq7m"
ignoreUntil = 2026-09-19T00:00:00Z
reason = "undici (dev, transitive via jsdom/test tooling at 6.26.0 & 7.26.0) — same dev-only test-environment exposure as GHSA-35p6-xmwp-9g52; not in any shipped artifact. Time-bounded to 2026-09-19; remediate via jsdom bump (V1C-18)."

[[IgnoredVulns]]
id = "GHSA-p88m-4jfj-68fv"
ignoreUntil = 2026-09-19T00:00:00Z
reason = "undici (dev, transitive via jsdom/test tooling at 6.26.0 & 7.26.0) — same dev-only test-environment exposure as GHSA-35p6-xmwp-9g52; not in any shipped artifact. Time-bounded to 2026-09-19; remediate via jsdom bump (V1C-18)."

[[IgnoredVulns]]
id = "GHSA-vxpw-j846-p89q"
ignoreUntil = 2026-09-19T00:00:00Z
reason = "undici (dev, transitive via jsdom/test tooling at 6.26.0 & 7.26.0) — same dev-only test-environment exposure as GHSA-35p6-xmwp-9g52; not in any shipped artifact. Time-bounded to 2026-09-19; remediate via jsdom bump (V1C-18)."

[[IgnoredVulns]]
id = "GHSA-hm92-r4w5-c3mj"
ignoreUntil = 2026-09-19T00:00:00Z
reason = "undici (dev, transitive via jsdom/test tooling at 7.26.0) — same dev-only test-environment exposure as GHSA-35p6-xmwp-9g52; not in any shipped artifact. Time-bounded to 2026-09-19; remediate via jsdom bump (V1C-18)."
